---
const { brand, city, address } = Astro.props;
---
<footer class="border-t border-white/5 bg-ink-950/50">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-10">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
      <div>
        <div class="font-semibold text-zinc-100">{brand}</div>
        <div class="text-sm text-zinc-400">{city} · {address}</div>
      </div>
      <div class="text-xs text-zinc-500">
        Demo conversion-first · Astro + Tailwind · Deploy Vercel
      </div>
    </div>
  </div>
</footer>
